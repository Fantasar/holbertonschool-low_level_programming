#include <stdio.h>
#include "main.h"

/**
 * mul - multiplie deux entiers.
 * @a: Une variable entier.
 * @b: Une variable entier.
 * Return: Le resultat mul
 */

int mul(int a, int b)

{
int mul = (a * b);
return (mul);

}

