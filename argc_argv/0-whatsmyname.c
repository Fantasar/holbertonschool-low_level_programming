#include<stdio.h>

/**
 * main - Fonction principal.
 *@argc: entier qui contient le nombre d'argument de la fonction.
 *@argv: Tableaux qui contient le nom des argument de la fonction.
 *Return: return 0 en cas de reussite.
*/

int main(int argc, char *argv[])
{
(void)argc;

printf("%s\n", argv[0]);

return (0);
}
