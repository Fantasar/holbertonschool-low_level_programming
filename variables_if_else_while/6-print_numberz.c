#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0
 */
int main(void)
{
	int chiffre;

	for (chiffre = 0 ;  chiffre < 20 ; chiffre++)
		putchar(chiffre);

	putchar('\n');

	return (0);
}
