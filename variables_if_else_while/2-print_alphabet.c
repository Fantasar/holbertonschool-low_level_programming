#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main (void)
{
	int putc(int c, FILE *stream);
	int putchar(int c);

	{
	putchar("{a.z}\n");
	}
	return (0);
}

